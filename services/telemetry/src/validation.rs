// Correctness validation will live here after the raw metrics path is stable.
// Keeping the module in place makes the next milestone explicit without mixing
// matching-engine correctness rules into latency aggregation.
